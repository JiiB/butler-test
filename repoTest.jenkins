#!/usr/bin/env groovy

multibranchPipelineJob('repo-test') {
    displayName('repo-test cidi')
    branchSources {
        github {
            id('repo-test') // IMPORTANT: use a constant and unique identifier
            repoOwner('rasch-dtc')
            repository('repo-test')            
            scanCredentialsId('poleposition-jenkins-github-secret')
        }
    }
    orphanedItemStrategy {
        discardOldItems {
            numToKeep(20)
        }
    }
}