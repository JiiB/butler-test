#!/usr/bin/env groovy

multibranchPipelineJob('test') {
    displayName('test cidi')
    branchSources {
        github {
            id('test') // IMPORTANT: use a constant and unique identifier
            repoOwner('rasch-dtc')
            repository('test')            
            scanCredentialsId('poleposition-jenkins-github-secret')
        }
    }
    orphanedItemStrategy {
        discardOldItems {
            numToKeep(20)
        }
    }
}